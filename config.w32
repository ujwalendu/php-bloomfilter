// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("bloomfilter", "for bloomfilter support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("bloomfilter", "enable bloomfilter support", "no");

if (PHP_BLOOMFILTER != "no") {
	EXTENSION("bloomfilter", "bloomfilter.c");
}

